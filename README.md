# cvrp_gen5_multi_axis_solver
This document is a ?pick up from here? snapshot for the Gen5_v5 solver + runner + viewer that we iterated on in this thread. 1) WHAT TO RUN (MAIN ENTRYPOINTS) CORE SOLVER PIPELINE - ‘gen5_multi_axis_solver_schedule_validated.py‘
